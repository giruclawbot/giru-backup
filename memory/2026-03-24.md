# Memory — 2026-03-24

## Proyecto: easy-calories (NUEVO)
- Repo: `giruclawbot/easy-calories` — Next.js 15 + Firebase + USDA API
- URL live: https://giru-easy-calories.web.app
- Firebase project: `giru-easy-calories`
- Firebase account: aiautomationt1@gmail.com
- Commit deploy: `e46efcd`
- Stack: Next.js 15 static export, Firebase Hosting, Firestore, Google Auth, Quagga2 barcode, Recharts, USDA FoodData API
- USDA calls: client-side directo (NEXT_PUBLIC_USDA_API_KEY), no API routes (incompatible con static export)
- Firestore rules desplegadas: solo el uid del usuario puede read/write sus propios docs
- **PENDIENTE**: añadir `giru-easy-calories.web.app` a Firebase Auth > Authorized Domains en la consola para que Google Sign-In funcione en producción

## easy-calories — versiones y fixes (continuación)

### v1.1.0 (commit `502ab19` develop / `8896eae` main)
- Calculadora Mifflin-St Jeor + TDEE con modal ⚙️ en dashboard
- Meta calórica personalizable persistida en localStorage
- Version badge `v1.1.0` en NavBar y footer
- GitFlow: branch `develop` + `CONTRIBUTING.md`
- GitHub Actions CI/CD: `.github/workflows/deploy.yml` + `ci.yml`
- Security: `.env.local` no trackeado, `src/lib/env.ts` valida vars
- Accessibility: aria-labels, roles en todos los componentes

### Fix: Firebase lazy init (commit `c3be432` develop / `39d52a7` main)
- **Root cause**: Firebase se inicializaba a nivel de módulo durante prerender → crash con `auth/invalid-api-key` en CI (keys placeholder)
- **Fix**: `src/lib/firebase.ts` — init lazy con `typeof window !== 'undefined'`, funciones `getFirebaseAuth()` y `getFirebaseDb()`
- Todos los componentes actualizados: AuthProvider, NavBar, login/page.tsx, firestore.ts

### Fix: Dashboard 404 / "dead page" (fix en next.config.ts + firebase.json)
- **Root cause**: `trailingSlash: true` en Next.js + Firebase Hosting rewrite `** → /index.html` redirigía `/dashboard/` al root index.html en vez del `dashboard/index.html` generado
- **Fix**: Eliminado `trailingSlash: true` de next.config.ts; firebase.json con `cleanUrls: true, trailingSlash: false`

### v1.2.0 (commit `76d87dc` develop / `ae8b084` main)
- **PWA**: service worker manual (`public/sw.js`, cache-first), `public/manifest.json`, `PWAInstaller.tsx` (banner beforeinstallprompt), iconos 192/512px, meta tags Apple
- **Nutrición**: NutritionFacts (8 campos: cal, protein, carbs, fat, fiber, sugar, sodium, cholesterol), preview macros en FoodSearch escalado por cantidad, P/C/G en MealList
- next-pwa NO usado (conflicto con output: 'export') — service worker manual en su lugar
- Iconos PWA: PNGs verdes lisos válidos — pueden reemplazarse con diseño real

### GitHub Secrets pendientes (para CI/CD funcione en Actions)
- NEXT_PUBLIC_FIREBASE_API_KEY, AUTH_DOMAIN, PROJECT_ID, STORAGE_BUCKET, MESSAGING_SENDER_ID, APP_ID
- NEXT_PUBLIC_USDA_API_KEY: C2w5zWGGJ4OmKnCf5p2o4Op25HFzvj6PYanNPTt8
- FIREBASE_SERVICE_ACCOUNT: JSON de Firebase Console → Service Accounts → Generate key

## Proyecto: nombres — estado al 2026-03-24
- Total nombres: 47 (31 previos + 16 nuevos: Fernando, Carlos, Antonio, Juan, Samantha, Mariel, Nerissa, Adria, Estefania, Alejandro, Gerardo, Rafael, Miguel, Abraham, Rene, Jose)
- Commit nombres nuevos: `b53cec6`, build 50 páginas

---

## easy-calories — versiones tarde (2026-03-24 continuación)

### v1.3.1 (commit `026e47d` develop / `0ba4a8a` main)
- CHANGELOG.md completo (v1.0.0→v1.3.1) con instrucciones para agentes IA
- AGENTS.md en el repo con checklist de release obligatorio
- 9 suites de tests / 63 tests con Jest + Testing Library — 90.42% branch coverage
- jest.config.ts, jest.setup.ts, __mocks__/fileMock.js
- Test files: goals, usda, env, CalorieCalculator, EditMealModal, CalorieChart, DayPicker, MealList, PWAInstaller
- CI actualizado con test:ci step (coverage ≥90% requerido)

### v1.4.0 (commit `845ea65` develop / `c041bdd` main)
- Goals a Firestore: `users/{uid}/settings/profile` con localStorage como caché offline-first
- CalorieCalculator: campos targetWeightKg + ratePerWeek (slow/moderate/fast)
- Timeline estimado en semanas/meses, floor 1200 kcal (WHO), warning <1500 kcal
- Debounce 400ms en FoodSearch, React.memo en CalorieChart, useMemo en last7Days
- Optimistic UI en removeMeal
- README reescrito completo

### v1.5.0 (commit `05eea18` develop / `fed181c` main)
- UserProfile en Firestore: weightKg, heightCm, age, sex, locale, goalDetails
- GoalDetails: calorieGoal, goalType, targetWeightKg, ratePerWeek, weeksToGoal, BMR, TDEE
- Nueva página /profile: datos personales + goal details + selector de idioma
- CalorieCalculator: initialValues prop, onGoalSet recibe GoalDetails completo
- Dashboard: carga UserProfile completo, goal details card, macro targets con progress bars
- Macro targets WHO/AMDR: proteína 1.8g×kg, carbs 45-65%, grasa 28%, fibra 14g/1000kcal, azúcar <10%
- i18n: I18nProvider, useI18n(), messages/es.json + en.json — 83 tests, 91.56% branch coverage

### v1.5.1 — EN PROGRESO (fix i18n wiring)
- Problema: useI18n() conectado en todos los archivos pero tests fallan porque CalorieCalculator usa useI18n sin provider
- Build: ✅ limpio
- Tests: ❌ 12 failing — CalorieCalculator tests usan `getByLabelText('Edad')` pero t() devuelve clave en vez de texto
- Root cause: tests no envuelven con I18nProvider, labels ahora son t('calculator.age') que sin provider da 'calculator.age'
- Fix pendiente: mock de I18nProvider en jest.setup.ts o wrapper en tests CalorieCalculator
- Archivos donde useI18n fue conectado: dashboard/page.tsx, NavBar.tsx, login/page.tsx, dashboard/add/page.tsx, CalorieCalculator.tsx

## Identifiers críticos
- easy-calories path local: /root/.openclaw/workspace/easy-calories/
- easy-calories URL: https://giru-easy-calories.web.app
- easy-calories Firebase project: giru-easy-calories
- Último commit: `05eea18` en develop (v1.5.0)
- v1.5.1 commit pendiente (fix tests)

## Fix pendiente tests v1.5.1
- En jest.setup.ts agregar mock global de useI18n:
  ```ts
  jest.mock('@/components/I18nProvider', () => ({
    useI18n: () => ({ t: (key: string) => {
      const map: Record<string,string> = {
        'calculator.title': '🧮 Calculadora de calorías',
        'calculator.calculate': 'Calcular mi meta',
        'calculator.useGoal': 'Usar esta meta',
        'calculator.age': 'Edad', 'calculator.weight': 'Peso actual (kg)',
        'calculator.height': 'Altura (cm)', ... 
      }
      return map[key] ?? key
    }, locale: 'es', setLocale: () => {} }),
    I18nProvider: ({ children }: { children: React.ReactNode }) => children,
  }))
  ```
- O mejor: importar y cargar mensajes es.json en jest.setup.ts y retornar la traducción real
